{
  "name": "german",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview --host",
    "validate": "node scripts/validate-vocab.mjs",
    "export": "node scripts/export-anki.mjs"
  },
  "dependencies": {
    "astro": "^7.3.1"
  }
}
