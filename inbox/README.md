# Inbox

Drop the raw files from Markus here (RTF, RTFD, PDF, whatever he sends), then ask Claude Code to
process the inbox. The procedure is in `CLAUDE.md` at the repo root.

Processed originals are moved to `archive/lesson-NN/`. Never delete anything from the archive:
the originals are the only way to reprocess a lesson if a mistake surfaces later.
